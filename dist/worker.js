import{definePlugin,runWorker}from"@paperclipai/plugin-sdk";import*as path2 from"node:path";import{spawn}from"node:child_process";import{mkdtemp}from"node:fs/promises";import{existsSync,readFileSync,statSync}from"node:fs";import{tmpdir as tmpdir2}from"node:os";import{join as join3,dirname}from"node:path";import{fileURLToPath}from"node:url";import{execSync}from"node:child_process";import{createHash}from"node:crypto";var SECCOMP_FILTER_SHA256_PIN="__PLA114_SECCOMP_FILTER_SHA256__";var SECCOMP_LOADER_SHA256_PIN="0fc1b58d38895fb2dc7be1464b1230344530aa7f168af9478fa47153e20f8be0";import*as path from"node:path";import{mkdir,writeFile}from"node:fs/promises";var MAX_INPUT_FILE_BYTES=50*1024*1024;var MAX_INPUT_FILES=3;var MAX_TOTAL_INPUT_BYTES=120*1024*1024;var MAX_REPO_PATH_LEN=512;var DEFAULT_INTAKE_CAPS={maxFileBytes:MAX_INPUT_FILE_BYTES,maxTotalBytes:MAX_TOTAL_INPUT_BYTES};var INTAKE_ALLOWED_PREFIXES=["user-uploads/","artifacts/"];var ENFORCE_USER_UPLOADS_TENANT_SCOPING=false;var COMPANY_ID_RE=/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/;function assertSafeCompanyId(companyId){if(typeof companyId!=="string"||companyId.length===0){return"callerCompanyId must be a non-empty string"}if(!COMPANY_ID_RE.test(companyId)){return"callerCompanyId has disallowed characters (allowed: A-Za-z0-9._-, no leading dot)"}return null}var IntakeError=class extends Error{kind;httpStatus;constructor(kind,message,httpStatus){super(message);this.name="IntakeError";this.kind=kind;this.httpStatus=httpStatus}};var BASENAME_RE=/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/;function assertSafeIntakePath(repoPath,callerCompanyId,opts={}){if(typeof repoPath!=="string"||repoPath.length===0){return"repoPath must be a non-empty string"}if(repoPath.length>MAX_REPO_PATH_LEN){return`repoPath exceeds ${MAX_REPO_PATH_LEN} characters`}if(repoPath.includes("\0"))return"repoPath must not contain NUL";if(repoPath.includes("\\"))return"repoPath must use forward slashes ('/'), not backslashes";if(repoPath.startsWith("/"))return"repoPath must be relative (no leading '/')";if(repoPath.endsWith("/"))return"repoPath must reference a file, not a directory (no trailing '/')";const normalized=path.posix.normalize(repoPath);if(normalized!==repoPath){return"repoPath would normalize differently (path traversal blocked)"}if(repoPath.split("/").some(seg=>seg==="..")){return"repoPath must not contain '..' segments"}if(!INTAKE_ALLOWED_PREFIXES.some(p=>normalized.startsWith(p))){return`repoPath must be under one of: ${INTAKE_ALLOWED_PREFIXES.join(", ")}`}const base=path.posix.basename(normalized);if(!base||base==="."||base===".."){return"repoPath must reference a file, not a directory"}if(!BASENAME_RE.test(base)){return"repoPath basename has disallowed characters (allowed: A-Za-z0-9._-, no leading dot)"}const cidErr=assertSafeCompanyId(callerCompanyId);if(cidErr)return`internal: ${cidErr}`;if(normalized.startsWith("artifacts/")){const tenantPrefix=`artifacts/${callerCompanyId}/`;if(!normalized.startsWith(tenantPrefix)){return`repoPath under 'artifacts/' must be within your tenant subtree '${tenantPrefix}' (cross-tenant read blocked)`}}else if(normalized.startsWith("user-uploads/")){const enforce=opts.enforceUserUploadsScoping??ENFORCE_USER_UPLOADS_TENANT_SCOPING;if(enforce){const tenantPrefix=`user-uploads/${callerCompanyId}/`;if(!normalized.startsWith(tenantPrefix)){return`repoPath under 'user-uploads/' must be within your tenant subtree '${tenantPrefix}' (cross-tenant read blocked)`}}}return null}function parseInputArtifacts(raw,callerCompanyId,opts={}){if(raw===void 0||raw===null)return{repoPaths:[]};if(!Array.isArray(raw))return{error:"inputArtifacts must be an array"};if(raw.length===0)return{repoPaths:[]};if(raw.length>MAX_INPUT_FILES){return{error:`inputArtifacts may contain at most ${MAX_INPUT_FILES} files`}}const repoPaths=[];const seenBasenames=new Set;for(let i=0;i<raw.length;i++){const item=raw[i];if(typeof item!=="object"||item===null||Array.isArray(item)){return{error:`inputArtifacts[${i}] must be an object { repoPath }`}}const keys=Object.keys(item);if(keys.some(k=>k!=="repoPath")){return{error:`inputArtifacts[${i}] has unexpected keys (only 'repoPath' allowed)`}}const repoPath=item.repoPath;const err=assertSafeIntakePath(repoPath,callerCompanyId,opts);if(err)return{error:`inputArtifacts[${i}].${err}`};const base=path.posix.basename(repoPath);if(seenBasenames.has(base)){return{error:`inputArtifacts[${i}] basename '${base}' collides with an earlier entry`}}seenBasenames.add(base);repoPaths.push(repoPath)}return{repoPaths}}function parseGitHubUrl(repoUrl){let u;try{u=new URL(repoUrl)}catch{throw new IntakeError("prerequisite_missing",`Cannot parse artifact repo URL: ${repoUrl}`)}if(u.protocol!=="https:"||u.host!=="github.com"){throw new IntakeError("prerequisite_missing",`artifactRepoUrl must be an https://github.com/<owner>/<repo>(.git) URL. Got: ${repoUrl}`)}const segs=u.pathname.replace(/^\/+/,"").replace(/\.git\/?$/,"").replace(/\/+$/,"").split("/");if(segs.length!==2||!segs[0]||!segs[1]){throw new IntakeError("prerequisite_missing",`Cannot parse owner/repo from ${repoUrl}`)}return[segs[0],segs[1]]}function encodeRepoPathForUrl(repoPath){return repoPath.split("/").map(encodeURIComponent).join("/")}var FETCH_TIMEOUT_MS=3e4;async function fetchInputArtifact(pat,repoUrl,branch,repoPath,fetchImpl=fetch,caps=DEFAULT_INTAKE_CAPS){const[owner,repo]=parseGitHubUrl(repoUrl);const encodedPath=encodeRepoPathForUrl(repoPath);const url=`https://api.github.com/repos/${encodeURIComponent(owner)}/${encodeURIComponent(repo)}/contents/${encodedPath}?ref=${encodeURIComponent(branch)}`;const headers={Authorization:`Bearer ${pat}`,Accept:"application/vnd.github.raw","X-GitHub-Api-Version":"2022-11-28","User-Agent":"paperclip-plugin-cad/intake"};let resp;try{resp=await fetchImpl(url,{headers,signal:AbortSignal.timeout(FETCH_TIMEOUT_MS)})}catch(err){throw new IntakeError("network",`Network error fetching input '${repoPath}': ${err.message}`)}if(resp.status===404){throw new IntakeError("not_found",`Input artifact not found (404): ${repoPath}`,404)}if(resp.status===401||resp.status===403){throw new IntakeError("auth",`Not authorized (${resp.status}) reading input '${repoPath}'. Verify the export PAT has read access to the artifact repo.`,resp.status)}if(!resp.ok){throw new IntakeError("network",`Unexpected ${resp.status} fetching input '${repoPath}'. Retry later.`,resp.status)}const clHeader=resp.headers.get("content-length");if(clHeader!==null){const cl=Number(clHeader);if(Number.isFinite(cl)&&cl>caps.maxFileBytes){throw new IntakeError("too_large",`Input '${repoPath}' is ${cl} bytes, over the ${caps.maxFileBytes}-byte per-file cap.`)}}const bytes=Buffer.from(await resp.arrayBuffer());if(bytes.byteLength>caps.maxFileBytes){throw new IntakeError("too_large",`Input '${repoPath}' is ${bytes.byteLength} bytes, over the ${caps.maxFileBytes}-byte per-file cap.`)}return{basename:path.posix.basename(repoPath),bytes}}async function fetchInputArtifacts(pat,repoUrl,branch,repoPaths,fetchImpl=fetch,caps=DEFAULT_INTAKE_CAPS){const files=[];let total=0;for(const repoPath of repoPaths){const file=await fetchInputArtifact(pat,repoUrl,branch,repoPath,fetchImpl,caps);total+=file.bytes.byteLength;if(total>caps.maxTotalBytes){throw new IntakeError("too_large",`Combined input size ${total} bytes exceeds the ${caps.maxTotalBytes}-byte total cap.`)}files.push(file)}return files}async function stageInputFiles(workdir,files){if(!files.length)return;const inputsDir=path.join(workdir,"inputs");await mkdir(inputsDir,{recursive:true});const inputsResolved=path.resolve(inputsDir);for(const f of files){const base=path.basename(f.basename);if(!base||base==="."||base===".."){throw new IntakeError("validation",`refusing to stage input with unsafe basename '${f.basename}'`)}const dest=path.join(inputsDir,base);const destResolved=path.resolve(dest);if(destResolved!==path.join(inputsResolved,base)){throw new IntakeError("validation",`refusing to stage input outside inputs/ dir: '${f.basename}'`)}await writeFile(dest,f.bytes)}}import{tmpdir}from"node:os";import{join as join2}from"node:path";var CadWorkerInternalError=class extends Error{code="worker_internal";constructor(message){super(message);this.name="CadWorkerInternalError"}};var ARTIFACT_STAGING_DIR=join2(tmpdir(),"paperclip-cad-staging");var GRACE_SECONDS=5;var BWRAP_OVERHEAD_GRACE_MS=100;var MAX_TIMEOUT_SECONDS=300;var DEFAULT_TIMEOUT_SECONDS=30;var __filename=fileURLToPath(import.meta.url);var __dirname=dirname(__filename);var WORKER_PY=join3(__dirname,"cad_worker.py");var SECCOMP_FILTER_PATH=join3(__dirname,"..","worker","seccomp_filter.bpf");var SECCOMP_LOADER_PATH=join3(__dirname,"..","worker","seccomp_load.py");var SANDBOX_ROOT="/sandbox";var SANDBOX_FILTER_PATH=`${SANDBOX_ROOT}/seccomp_filter.bpf`;var SANDBOX_LOADER_PATH=`${SANDBOX_ROOT}/seccomp_load.py`;var SANDBOX_WORKER_PATH=`${SANDBOX_ROOT}/cad_worker.py`;var PREEXEC_PATH=join3(__dirname,"..","worker","cad_preexec");function defaultRlimits(timeoutSeconds){return{asBytes:2*1024**3,nproc:64,nofile:256,fsizeBytes:256*1024**2,cpuSeconds:timeoutSeconds+5,coreBytes:0}}function which(bin){try{const out=execSync(`command -v ${bin}`,{encoding:"utf8",stdio:["ignore","pipe","ignore"]}).trim();return out||null}catch{return null}}function bwrapVersionOf(bwrapPath){try{const out=execSync(`${bwrapPath} --version`,{encoding:"utf8",stdio:["ignore","pipe","ignore"]}).trim();const m=/(\d+)\.(\d+)/.exec(out);if(!m)return null;return{major:Number(m[1]),minor:Number(m[2])}}catch{return null}}function selectSpawnMode(env=process.env,platform=process.platform){const unsafeDev=env.CAD_WORKER_UNSAFE_DEV==="1";const isProd=env.NODE_ENV==="production";if(unsafeDev&&!isProd){return{mode:"dev_direct"}}if(platform!=="linux"){throw new CadWorkerInternalError("Option B sandbox unavailable: requires Linux + bwrap. Set CAD_WORKER_UNSAFE_DEV=1 (NODE_ENV must NOT be 'production') to run with the in-process layer only on developer machines.")}const bwrapPath=which("bwrap");if(!bwrapPath){throw new CadWorkerInternalError("Option B sandbox unavailable: 'bwrap' not on PATH. Install bubblewrap on the deploy host (apt-get install bubblewrap). Set CAD_WORKER_UNSAFE_DEV=1 (non-production only) to run direct.")}if(!existsSync(SECCOMP_FILTER_PATH)){throw new CadWorkerInternalError(`Option B sandbox unavailable: seccomp filter blob not found at ${SECCOMP_FILTER_PATH}. Build it with \`make -C worker seccomp_filter.bpf\` (requires libseccomp-dev).`)}if(!existsSync(SECCOMP_LOADER_PATH)){throw new CadWorkerInternalError(`Option B sandbox unavailable: python seccomp loader not found at ${SECCOMP_LOADER_PATH}. This file ships in worker/ alongside the filter source.`)}const v=bwrapVersionOf(bwrapPath);const native=false;if(!native&&!existsSync(PREEXEC_PATH)){throw new CadWorkerInternalError(`bwrap ${v?.major}.${v?.minor} predates --rlimit-* (need 0.6+). Build the preexec wrapper with \`make -C worker cad_preexec\`, or upgrade bubblewrap on the deploy host.`)}const decision={mode:"bwrap+seccomp",bwrapPath,bwrapVersion:v??void 0,bwrapHasNativeRlimits:native,seccompFilterPath:SECCOMP_FILTER_PATH,seccompLoaderPath:SECCOMP_LOADER_PATH,preexecPath:native?void 0:PREEXEC_PATH};verifySeccompPins(decision);return decision}var SHA256_HEX_LEN=64;var SHA256_HEX_RE=/^[0-9a-f]{64}$/i;function readSidecarSha(name){const sidecarPath=join3(__dirname,"..","dist",`${name}.sha256`);if(!existsSync(sidecarPath))return void 0;const raw=readFileSync(sidecarPath,"utf8").trim();return SHA256_HEX_RE.test(raw)?raw.toLowerCase():void 0}function resolveDefaultPins(){const filterConst=SECCOMP_FILTER_SHA256_PIN.length===SHA256_HEX_LEN?SECCOMP_FILTER_SHA256_PIN:void 0;const loaderConst=SECCOMP_LOADER_SHA256_PIN.length===SHA256_HEX_LEN?SECCOMP_LOADER_SHA256_PIN:void 0;return{filterSha256:filterConst??readSidecarSha("seccomp_filter.bpf")??SECCOMP_FILTER_SHA256_PIN,loaderSha256:loaderConst??readSidecarSha("seccomp_load.py")??SECCOMP_LOADER_SHA256_PIN}}function verifySeccompPins(decision,pins=resolveDefaultPins()){if(decision.mode!=="bwrap+seccomp")return;const checks=[{name:"seccomp_filter.bpf",pin:pins.filterSha256,path:decision.seccompFilterPath??""},{name:"seccomp_load.py",pin:pins.loaderSha256,path:decision.seccompLoaderPath??""}];for(const c of checks){if(c.pin.length!==SHA256_HEX_LEN){throw new CadWorkerInternalError(`[114 \xA75.2] ${c.name}: build manifest unsubstituted \u2014 pin length ${c.pin.length} \u2260 ${SHA256_HEX_LEN} (placeholder __PLA114_SECCOMP_*_SHA256__ still present?). Run \`npm run build\` so esbuild substitutes the digests.`)}if(!SHA256_HEX_RE.test(c.pin)){throw new CadWorkerInternalError(`[114 \xA75.2] ${c.name}: build manifest pin is not a sha256 hex string: ${c.pin}`)}if(!c.path){throw new CadWorkerInternalError(`[114 \xA75.2] ${c.name}: SpawnModeDecision is missing the path field \u2014 cannot verify pin.`)}let actual;try{actual=createHash("sha256").update(readFileSync(c.path)).digest("hex")}catch(err){throw new CadWorkerInternalError(`[114 \xA75.2] ${c.name}: failed to read for sha256 verification (path=${c.path}): ${err.message}`)}if(actual.toLowerCase()!==c.pin.toLowerCase()){throw new CadWorkerInternalError(`[114 \xA75.2] ${c.name}: sha256 mismatch \u2014 manifest pin=${c.pin} actual=${actual} path=${c.path}. Refusing to launch worker; the kernel sandbox layer would be silently inert under this state (substitution-attack defense).`)}}}var PYTHON_BOOTSTRAP="import sys; sys.path.insert(0, '/sandbox'); from seccomp_load import lock_down; lock_down('/sandbox/seccomp_filter.bpf'); import cad_worker; cad_worker.main()";function buildSpawnInvocation(opts){const pythonBin=opts.pythonBin??"python3";const env={PATH:"/usr/bin:/bin",PYTHONDONTWRITEBYTECODE:"1",PYTHONUNBUFFERED:"1"};if(opts.decision.mode==="dev_direct"){return{command:pythonBin,args:[WORKER_PY],env,stdio:["pipe","pipe","pipe"]}}const bwrap=opts.decision.bwrapPath;const venvPython=pythonBin;const filterBlob=opts.decision.seccompFilterPath;const loaderShim=opts.decision.seccompLoaderPath;const args=["--unshare-all","--die-with-parent","--new-session","--clearenv","--setenv","PATH","/usr/bin:/bin","--setenv","HOME","/tmp","--setenv","LANG","C.UTF-8","--setenv","PYTHONDONTWRITEBYTECODE","1","--setenv","PYTHONHASHSEED","random","--setenv","PYTHONUNBUFFERED","1","--uid","65534","--gid","65534","--hostname","cad-worker","--proc","/proc","--dev","/dev","--ro-bind","/usr","/usr","--ro-bind","/lib","/lib","--ro-bind","/lib64","/lib64","--ro-bind","/bin","/bin","--ro-bind","/etc/ld.so.cache","/etc/ld.so.cache","--ro-bind",filterBlob,SANDBOX_FILTER_PATH,"--ro-bind",loaderShim,SANDBOX_LOADER_PATH,"--ro-bind",WORKER_PY,SANDBOX_WORKER_PATH,"--tmpfs","/tmp","--bind",opts.workdir,opts.workdir,"--chdir",opts.workdir,"--cap-drop","ALL"];if(opts.decision.bwrapHasNativeRlimits){args.push("--rlimit-as",String(opts.rlimits.asBytes),"--rlimit-nproc",String(opts.rlimits.nproc),"--rlimit-nofile",String(opts.rlimits.nofile),"--rlimit-fsize",String(opts.rlimits.fsizeBytes),"--rlimit-cpu",String(opts.rlimits.cpuSeconds),"--rlimit-core",String(opts.rlimits.coreBytes));args.push("--",venvPython,"-c",PYTHON_BOOTSTRAP)}else{const preexec=opts.decision.preexecPath;args.push("--ro-bind",preexec,preexec);args.push("--setenv","CAD_PREEXEC_RLIMIT_AS",String(opts.rlimits.asBytes));args.push("--setenv","CAD_PREEXEC_RLIMIT_NPROC",String(opts.rlimits.nproc));args.push("--setenv","CAD_PREEXEC_RLIMIT_NOFILE",String(opts.rlimits.nofile));args.push("--setenv","CAD_PREEXEC_RLIMIT_FSIZE",String(opts.rlimits.fsizeBytes));args.push("--setenv","CAD_PREEXEC_RLIMIT_CPU",String(opts.rlimits.cpuSeconds));args.push("--setenv","CAD_PREEXEC_RLIMIT_CORE",String(opts.rlimits.coreBytes));args.push("--",preexec,venvPython,"-c",PYTHON_BOOTSTRAP)}const stdio=["pipe","pipe","pipe"];return{command:bwrap,args,env,stdio}}async function invokeWorker(job,timeoutSeconds,decision=selectSpawnMode(),pythonBin="python3"){const rlimits=defaultRlimits(timeoutSeconds);const invocation=buildSpawnInvocation({decision,workdir:job.workdir,pythonBin,rlimits});return new Promise(resolve2=>{const child=spawn(invocation.command,invocation.args,{stdio:invocation.stdio,env:invocation.env});let stdout="";let stderr="";let settled=false;let killTimer=null;const settle=result=>{if(settled)return;settled=true;if(killTimer!==null)clearTimeout(killTimer);resolve2(result)};const overheadMs=decision.mode==="bwrap+seccomp"?BWRAP_OVERHEAD_GRACE_MS:0;killTimer=setTimeout(()=>{if(settled)return;settled=true;try{child.kill("SIGKILL")}catch{}resolve2({ok:false,error:"worker_timeout",message:`CAD script timed out after ${timeoutSeconds}s`})},timeoutSeconds*1e3+overheadMs+GRACE_SECONDS*1e3);if(child.stdout){child.stdout.on("data",chunk=>{stdout+=chunk.toString("utf8")})}if(child.stderr){child.stderr.on("data",chunk=>{stderr+=chunk.toString("utf8")})}child.on("close",(code,signal)=>{if(settled)return;if(killTimer!==null)clearTimeout(killTimer);if(signal==="SIGSYS"){settle({ok:false,error:"sandbox_violation",message:`Worker killed by seccomp (SIGSYS). stderr: ${stderr.slice(0,500)}`,exitSignal:signal,exitCode:code});return}if(signal==="SIGKILL"&&/seccomp/i.test(stderr)){settle({ok:false,error:"sandbox_violation",message:`Worker killed by kernel (SIGKILL with seccomp audit line). stderr: ${stderr.slice(0,500)}`,exitSignal:signal,exitCode:code});return}const line=stdout.trim();if(!line){settle({ok:false,error:"worker_internal",message:`Worker produced no output on stdout. code=${code} signal=${signal} stderr: ${stderr.slice(0,500)}`,exitSignal:signal,exitCode:code});return}const newlineIdx=line.indexOf("\n");const firstLine=newlineIdx===-1?line:line.slice(0,newlineIdx);try{const parsed=JSON.parse(firstLine);if(!parsed.ok){parsed.exitSignal=signal;parsed.exitCode=code}settle(parsed)}catch{settle({ok:false,error:"worker_internal",message:`Worker output was not valid JSON: ${firstLine.slice(0,200)}`,exitSignal:signal,exitCode:code})}});child.on("error",err=>{settle({ok:false,error:"worker_internal",message:`Failed to spawn worker process: ${err.message}`})});const jobJson=JSON.stringify(job);if(child.stdin){child.stdin.write(jobJson,"utf8",()=>{child.stdin?.end()})}})}async function renderCadQuery(script,format,timeoutSeconds=DEFAULT_TIMEOUT_SECONDS,decision=selectSpawnMode(),inputFiles){const effectiveTimeout=Math.min(Math.max(1,timeoutSeconds),MAX_TIMEOUT_SECONDS);const workdir=await mkdtemp(join3(tmpdir2(),"cad-worker-"));if(inputFiles?.length)await stageInputFiles(workdir,inputFiles);return invokeWorker({script,format,workdir},effectiveTimeout,decision)}var DEFAULT_ARTIFACT_REPO_URL="https://github.com/claudegoogl-sudo/cad-artifacts.git";var DEFAULT_ARTIFACT_BRANCH="main";var artifactStagingMap=new Map;var MAX_RETAINED_INPUT_BYTES=2*MAX_TOTAL_INPUT_BYTES;function entryInputBytes(entry){let n=0;for(const f of entry.inputs??[])n+=f.bytes.byteLength;return n}function enforceRetainedInputCap(map=artifactStagingMap,cap=MAX_RETAINED_INPUT_BYTES){let total=0;for(const entry of map.values())total+=entryInputBytes(entry);if(total<=cap)return;for(const entry of map.values()){if(total<=cap)break;const freed=entryInputBytes(entry);if(freed===0)continue;entry.inputs=void 0;total-=freed}}var MAX_STAGING_ENTRIES=256;function enforceStagingEntryCap(map=artifactStagingMap,cap=MAX_STAGING_ENTRIES){if(map.size<=cap)return;for(const key of map.keys()){if(map.size<=cap)break;map.delete(key)}}var MAX_SCRIPT_BYTES=256*1024;function stagingMapKey(companyId,agentId,artifactId){return`${companyId}:${agentId}:${artifactId}`}var PushError=class extends Error{kind;httpStatus;constructor(kind,message,httpStatus){super(message);this.name="PushError";this.kind=kind;this.httpStatus=httpStatus}};function validationError(message){return{error:"validation_error",data:{code:"validation_error",statusCode:400,message}}}function workerInternalError(message){return{data:{code:"worker_internal",statusCode:500,message}}}async function emitMetrics(ctx,tool,durationMs,isError){await ctx.metrics?.write("tool.calls",1,{tool});await ctx.metrics?.write("tool.duration_ms",durationMs,{tool});if(isError)await ctx.metrics?.write("tool.errors",1,{tool})}function logCompletion(ctx,tool,runCtx,durationMs,status){ctx.logger.info("tool call complete",{correlationId:runCtx.runId,tool,agentId:runCtx.agentId,status,durationMs})}var TICKET_ID_RE=/^[A-Z][A-Z0-9]{1,9}-[0-9]{1,9}$/;var TOOL_CALL_ID_RE=/^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$/;var FILENAME_RE=/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/;function validateTicketId(value){if(!TICKET_ID_RE.test(value)){return"paperclipTicketId must match ^[A-Z][A-Z0-9]{1,9}-[0-9]{1,9}$ (e.g. 56)"}return null}function validateToolCallId(value){if(!TOOL_CALL_ID_RE.test(value)){return"toolCallId must match ^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$"}return null}function validateFilename(value){if(value.startsWith(".")||value.includes("..")){return"filename must not start with '.' or contain '..'"}if(!FILENAME_RE.test(value)){return"filename must match ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"}return null}function assertSafeRepoPath(repoPath,companyId){const normalized=path2.posix.normalize(repoPath);if(normalized!==repoPath)return"internal: repoPath would normalize differently (path traversal blocked)";const tenantPrefix=`artifacts/${companyId}/`;if(!normalized.startsWith(tenantPrefix)){return`internal: repoPath must start with '${tenantPrefix}' (tenant scoping)`}return null}function encodeRepoPathForUrl2(repoPath){return repoPath.split("/").map(encodeURIComponent).join("/")}var FETCH_TIMEOUT_MS2=3e4;function fetchSignal(){return AbortSignal.timeout(FETCH_TIMEOUT_MS2)}function githubHeaders(pat){return{Authorization:`Bearer ${pat}`,Accept:"application/vnd.github+json","X-GitHub-Api-Version":"2022-11-28","Content-Type":"application/json","User-Agent":"paperclip-plugin-cad/0.1.0"}}function parseGitHubUrl2(repoUrl){let u;try{u=new URL(repoUrl)}catch{throw new PushError("prerequisite_missing",`Cannot parse GitHub URL: ${repoUrl}`)}if(u.protocol!=="https:"||u.host!=="github.com"){throw new PushError("prerequisite_missing",`artifactRepoUrl must be an https://github.com/<owner>/<repo>(.git) URL. Got: ${repoUrl}`)}const segs=u.pathname.replace(/^\/+/,"").replace(/\.git\/?$/,"").replace(/\/+$/,"").split("/");if(segs.length!==2||!segs[0]||!segs[1]){throw new PushError("prerequisite_missing",`Cannot parse owner/repo from ${repoUrl}`)}return[segs[0],segs[1]]}async function checkRepoPrerequisite(pat,repoUrl){const[owner,repo]=parseGitHubUrl2(repoUrl);const headers=githubHeaders(pat);let resp;try{resp=await fetch(`https://api.github.com/repos/${encodeURIComponent(owner)}/${encodeURIComponent(repo)}`,{headers,signal:fetchSignal()})}catch(err){throw new PushError("network",`Network error reaching ${owner}/${repo}: ${err.message}`)}if(resp.status===404){throw new PushError("prerequisite_missing",`Artifact repo not found (404): ${owner}/${repo}. Operator must pre-create the repo and grant PAT access. See 56 AC#1.`,404)}if(resp.status===401||resp.status===403){throw new PushError("prerequisite_missing",`Artifact repo not accessible (${resp.status}): ${owner}/${repo}. Verify PAT has repo scope. See 56 AC#1.`,resp.status)}if(!resp.ok){throw new PushError("network",`Unexpected ${resp.status} checking ${owner}/${repo}. Retry later.`,resp.status)}}async function checkArtifactExists(pat,repoUrl,repoPath){let owner,repo;try{[owner,repo]=parseGitHubUrl2(repoUrl)}catch{return null}const headers=githubHeaders(pat);const encodedPath=encodeRepoPathForUrl2(repoPath);const ownerEnc=encodeURIComponent(owner);const repoEnc=encodeURIComponent(repo);let contentsResp;try{contentsResp=await fetch(`https://api.github.com/repos/${ownerEnc}/${repoEnc}/contents/${encodedPath}`,{headers,signal:fetchSignal()})}catch{return null}if(!contentsResp.ok)return null;const contentsData=await contentsResp.json();try{const commitResp=await fetch(`https://api.github.com/repos/${ownerEnc}/${repoEnc}/commits?path=${encodeURIComponent(repoPath)}&per_page=1`,{headers,signal:fetchSignal()});if(commitResp.ok){const commits=await commitResp.json();const sha=commits[0]?.sha;if(sha)return{commitSha:sha,permalink:`https://github.com/${owner}/${repo}/blob/${sha}/${repoPath}`}}}catch{}return{commitSha:contentsData.sha??"unknown",permalink:contentsData.html_url??`https://github.com/${owner}/${repo}/blob/main/${repoPath}`}}async function pushArtifactToGitHub(pat,repoUrl,branch,localFile,repoPath,message){const[owner,repo]=parseGitHubUrl2(repoUrl);const{readFile}=await import("node:fs/promises");const contentBase64=(await readFile(localFile)).toString("base64");const encodedPath=encodeRepoPathForUrl2(repoPath);const apiBase=`https://api.github.com/repos/${encodeURIComponent(owner)}/${encodeURIComponent(repo)}/contents/${encodedPath}`;const headers=githubHeaders(pat);let existingSha;let getResp;try{getResp=await fetch(apiBase,{headers,signal:fetchSignal()})}catch(err){throw new PushError("network",`Network error fetching ${repoPath}: ${err.message}`)}if(getResp.ok){existingSha=(await getResp.json()).sha}else if(getResp.status===401||getResp.status===403){throw new PushError("auth",`Auth failed (${getResp.status}) reading ${repoPath}. Rotate PAT.`,getResp.status)}else if(getResp.status>=500){throw new PushError("network",`API ${getResp.status} reading ${repoPath}. Retry.`,getResp.status)}const body={message,content:contentBase64,branch};if(existingSha)body.sha=existingSha;let putResp;try{putResp=await fetch(apiBase,{method:"PUT",headers,body:JSON.stringify(body),signal:fetchSignal()})}catch(err){throw new PushError("network",`Network error pushing ${repoPath}: ${err.message}`)}if(!putResp.ok){const s=putResp.status;if(s===401||s===403)throw new PushError("auth",`Push auth failed (${s}). Rotate PAT.`,s);if(s===409||s===422)throw new PushError("conflict",`Conflict (${s}) on ${repoPath}.`,s);if(s>=500)throw new PushError("network",`API ${s} pushing ${repoPath}. Retry.`,s);throw new PushError("network",`API error ${s}: ${await putResp.text()}`,s)}const result=await putResp.json();const commitSha=result.commit?.sha??"";return{commitSha,permalink:`https://github.com/${owner}/${repo}/blob/${commitSha}/${repoPath}`}}async function renderCadScript(script,timeoutSeconds=DEFAULT_TIMEOUT_SECONDS,inputFiles){const result=await renderCadQuery(script,"step",timeoutSeconds,void 0,inputFiles);if(!result.ok)throw new Error(`[${result.error}] ${result.message}`);return result.artifactPath}async function exportToFormat(entry,format){if(format==="step")return entry.stepPath;const result=await renderCadQuery(entry.script,format,DEFAULT_TIMEOUT_SECONDS,void 0,entry.inputs);if(!result.ok)throw new Error(`[${result.error}] Export to ${format} failed: ${result.message}`);return result.artifactPath}var plugin=definePlugin({async setup(ctx){ctx.logger.info("CAD plugin worker starting");const anyCtx=ctx;ctx.tools.register("cad.run_script",{displayName:"CAD Run Script",description:"Execute a CadQuery Python script. Returns { artifactId, summary }.",parametersSchema:{type:"object",properties:{script:{type:"string",maxLength:MAX_SCRIPT_BYTES,description:"CadQuery Python script (max 256 KiB)."},timeout:{type:"integer",minimum:1,maximum:300,description:"Timeout (seconds, default 30)."},inputArtifacts:{type:"array",maxItems:MAX_INPUT_FILES,items:{type:"object",properties:{repoPath:{type:"string",description:"Path of an uploaded scan in the cad-artifacts repo (under user-uploads/, or your own tenant's artifacts/<companyId>/ subtree \u2014 cross-tenant artifacts/ paths are rejected). Fetched by the host and staged into the sandbox at inputs/<basename>; read it via StlAPI_Reader('inputs/<basename>')."}},required:["repoPath"],additionalProperties:false},description:"Optional scan/mesh files to stage into the sandbox before the script runs (1089). Per-file 50 MiB cap, 120 MiB total, max 3 files."}},required:["script"],additionalProperties:false}},async(params,runCtxRaw)=>{const runCtx=runCtxRaw??{};const tool="cad.run_script";const t0=Date.now();if(typeof params!=="object"||params===null){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError("params must be an object")}const p=params;if(typeof p.script!=="string"||p.script.length===0){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError("script is required and must be a non-empty string")}if(Buffer.byteLength(p.script,"utf8")>MAX_SCRIPT_BYTES){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError(`script exceeds maximum size of ${MAX_SCRIPT_BYTES} bytes`)}if(p.timeout!==void 0){const t=p.timeout;if(typeof t!=="number"||!Number.isInteger(t)||t<1||t>300){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError("timeout must be an integer between 1 and 300")}}const script=p.script;const timeoutSeconds=typeof p.timeout==="number"?p.timeout:DEFAULT_TIMEOUT_SECONDS;if(typeof runCtx.companyId!=="string"||runCtx.companyId.length===0||typeof runCtx.agentId!=="string"||runCtx.agentId.length===0){ctx.logger.warn("cad.run_script: missing tenant context on runCtx");const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError("missing tenant context (companyId/agentId) on runCtx")}let inputFiles=[];const parsedInputs=parseInputArtifacts(p.inputArtifacts,runCtx.companyId);if("error"in parsedInputs){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return validationError(parsedInputs.error)}if(parsedInputs.repoPaths.length>0){const config=await ctx.config.get();const intakeSecretId=config.intakePatSecretId??config.githubPatSecretId;if(!intakeSecretId){const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return{data:{error:"prerequisite_missing",message:"inputArtifacts requires githubPatSecretId (or intakePatSecretId) to be configured."}}}const repoUrl=config.artifactRepoUrl??DEFAULT_ARTIFACT_REPO_URL;const branch=config.artifactRepoBranch??DEFAULT_ARTIFACT_BRANCH;ctx.logger.info("cad.run_script: fetching inputArtifacts",{count:parsedInputs.repoPaths.length,intakePat:config.intakePatSecretId?"dedicated":"shared"});const pat=await ctx.secrets.resolve(intakeSecretId);try{inputFiles=await fetchInputArtifacts(pat,repoUrl,branch,parsedInputs.repoPaths)}catch(err){if(err instanceof IntakeError){ctx.logger.warn("cad.run_script: intake fetch failed",{kind:err.kind,httpStatus:err.httpStatus});const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return{data:{error:err.kind,message:err.message}}}throw err}}ctx.logger.info("cad.run_script: rendering",{scriptLength:script.length,timeoutSeconds,inputCount:inputFiles.length});let stepPath;try{stepPath=await renderCadScript(script,timeoutSeconds,inputFiles)}catch(err){const msg=err instanceof Error?err.message:"Unknown worker error";ctx.logger.warn("cad.run_script: worker error",{error:msg});const ms2=Date.now()-t0;await emitMetrics(anyCtx,tool,ms2,true);logCompletion(ctx,tool,runCtx,ms2,"error");return workerInternalError(msg)}const artifactId=`cad-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;artifactStagingMap.set(stagingMapKey(runCtx.companyId,runCtx.agentId,artifactId),{script,stepPath,inputs:inputFiles.length>0?inputFiles:void 0});enforceStagingEntryCap();enforceRetainedInputCap();ctx.logger.info("cad.run_script: staged",{artifactId});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,false);logCompletion(ctx,tool,runCtx,ms,"ok");return{content:`Artifact staged: ${artifactId}`,data:{artifactId,summary:`CadQuery script executed successfully (${script.length} chars)`}}});ctx.tools.register("cad.export",{displayName:"CAD Export",description:"Export a staged CAD artifact to the configured GitHub artifact repo. Returns { commitSha, permalink, artifactPath }. Idempotent per toolCallId.",parametersSchema:{type:"object",properties:{artifactId:{type:"string",description:"Artifact ID from cad.run_script."},format:{type:"string",enum:["step","stl","3mf","dxf","svg"],description:"Output format."},paperclipTicketId:{type:"string",pattern:"^[A-Z][A-Z0-9]{1,9}-[0-9]{1,9}$",description:"Paperclip ticket ID (e.g. 56) for path/commit message."},toolCallId:{type:"string",pattern:"^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$",description:"Tool-call ID for deterministic path and idempotency."},filename:{type:"string",pattern:"^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",description:"Optional filename override. Default: artifact.<format>."}},required:["artifactId","format","paperclipTicketId","toolCallId"],additionalProperties:false}},async(params,runCtxRaw)=>{const runCtx=runCtxRaw??{};const tool="cad.export";const t0=Date.now();if(typeof params!=="object"||params===null){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError("params must be an object")}const p=params;if(typeof p.artifactId!=="string"||p.artifactId.length===0){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError("artifactId is required and must be a non-empty string")}const validFormats=["step","stl","3mf","dxf","svg"];if(typeof p.format!=="string"||!validFormats.includes(p.format)){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(`format must be one of: ${validFormats.join(", ")}`)}const{artifactId,format,paperclipTicketId,toolCallId,filename}=p;if(paperclipTicketId!==void 0){if(typeof paperclipTicketId!=="string"){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError("paperclipTicketId must be a string")}const tErr=validateTicketId(paperclipTicketId);if(tErr){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(tErr)}}if(toolCallId!==void 0){if(typeof toolCallId!=="string"){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError("toolCallId must be a string")}const cErr=validateToolCallId(toolCallId);if(cErr){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(cErr)}}if(filename!==void 0){if(typeof filename!=="string"){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError("filename must be a string")}const fErr=validateFilename(filename);if(fErr){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(fErr)}}ctx.logger.info("cad.export: starting",{artifactId,format});const hasTenantCtx=typeof runCtx.companyId==="string"&&runCtx.companyId.length>0&&typeof runCtx.agentId==="string"&&runCtx.agentId.length>0;const stagingEntry=hasTenantCtx?artifactStagingMap.get(stagingMapKey(runCtx.companyId,runCtx.agentId,artifactId)):void 0;if(!stagingEntry){ctx.logger.warn("cad.export: unknown artifactId",{artifactId});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return workerInternalError(`No staged artifact for artifactId: ${artifactId}. Call cad.run_script first.`)}if(!paperclipTicketId||!toolCallId){try{const filePath=await exportToFormat(stagingEntry,format);const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,false);logCompletion(ctx,tool,runCtx,ms,"ok");return{data:{filePath,artifactId,format}}}catch(err){const msg=err instanceof Error?err.message:"Export failed";const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return workerInternalError(msg)}}const config=await ctx.config.get();if(!config.githubPatSecretId){const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return{data:{error:"prerequisite_missing",message:"githubPatSecretId not configured."}}}const repoUrl=config.artifactRepoUrl??DEFAULT_ARTIFACT_REPO_URL;const branch=config.artifactRepoBranch??DEFAULT_ARTIFACT_BRANCH;const ticketIdStr=paperclipTicketId;const toolCallIdStr=toolCallId;const filenameRaw=filename;const resolvedFilename=filenameRaw??`artifact.${format}`;const companyId=runCtx.companyId;const cidErr=assertSafeCompanyId(companyId);if(cidErr){ctx.logger.warn("cad.export: companyId assertion failed",{cidErr});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(`internal: ${cidErr}`)}const repoPath=`artifacts/${companyId}/${ticketIdStr}/${toolCallIdStr}/${resolvedFilename}`;const pathErr=assertSafeRepoPath(repoPath,companyId);if(pathErr){ctx.logger.warn("cad.export: repoPath assertion failed",{pathErr});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return validationError(pathErr)}ctx.logger.info("cad.export: resolving GitHub PAT");const pat=await ctx.secrets.resolve(config.githubPatSecretId);try{await checkRepoPrerequisite(pat,repoUrl)}catch(err){if(err instanceof PushError&&err.kind==="prerequisite_missing"){ctx.logger.warn("cad.export: prerequisite failed",{repoUrl,httpStatus:err.httpStatus});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return{data:{error:"prerequisite_missing",message:err.message}}}throw err}ctx.logger.info("cad.export: idempotency check",{repoPath});const existing=await checkArtifactExists(pat,repoUrl,repoPath);if(existing){ctx.logger.info("cad.export: already exists",{repoPath,commitSha:existing.commitSha});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,false);logCompletion(ctx,tool,runCtx,ms,"ok");return{content:`Artifact already present at ${repoPath} (${existing.commitSha})`,data:{commitSha:existing.commitSha,permalink:existing.permalink,artifactPath:repoPath}}}let localFile;try{localFile=await exportToFormat(stagingEntry,format)}catch(err){const msg=err instanceof Error?err.message:"Export failed";const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return workerInternalError(msg)}const commitMessage=`CAD artifact: ticket=${ticketIdStr} tool=cad.export call=${toolCallIdStr}`;const doPush=()=>pushArtifactToGitHub(pat,repoUrl,branch,localFile,repoPath,commitMessage);ctx.logger.info("cad.export: pushing",{repoPath,branch});try{let pushResult;try{pushResult=await doPush()}catch(firstErr){if(firstErr instanceof PushError&&firstErr.kind==="conflict"){ctx.logger.warn("cad.export: conflict, retrying once",{repoPath});pushResult=await doPush()}else{throw firstErr}}ctx.logger.info("cad.export: committed",{repoPath,commitSha:pushResult.commitSha});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,false);logCompletion(ctx,tool,runCtx,ms,"ok");return{content:`Artifact committed: ${pushResult.permalink}`,data:{commitSha:pushResult.commitSha,permalink:pushResult.permalink,artifactPath:repoPath}}}catch(err){if(err instanceof PushError){ctx.logger.warn("cad.export: push failed",{kind:err.kind,httpStatus:err.httpStatus});const ms=Date.now()-t0;await emitMetrics(anyCtx,tool,ms,true);logCompletion(ctx,tool,runCtx,ms,"error");return{data:{error:err.kind,message:err.message}}}throw err}});ctx.logger.info("CAD plugin worker setup complete")},async onHealth(){return{status:"ok",message:"CAD plugin worker is running"}}});var worker_default=plugin;runWorker(plugin,import.meta.url);export{MAX_RETAINED_INPUT_BYTES,MAX_SCRIPT_BYTES,MAX_STAGING_ENTRIES,worker_default as default,enforceRetainedInputCap,enforceStagingEntryCap};
